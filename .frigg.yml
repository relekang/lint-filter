tasks:
  setup:
    - npm install
  tests:
    - npm run coverage
    - npm run lint
    - npm run flow
    - ./integration-test

coverage:
  path: coverage/cobertura-coverage.xml
  parser: cobertura
